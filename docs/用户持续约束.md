用户持续约束（2026-09-09）：Clash 由用户自己管理。本任务不得操作、配置、开关、重启 Clash，也不得切换系统代理、修改路由、DNS 或代理模式。网络异常时保留现场；项目自身子进程可沿用已有代理环境，但不得据此修改 Clash 或系统网络设置。

用户已授权本项目正常的应用访问确认由 Codex 直接处理。该授权不扩大到本项目以外的操作。

2026-09-09 用户随后明确：上述“本项目”指 Codex-Control-MCP 服务整体，不限于 AI 短剧。用户信任经其认证授权的 MCP 连接，希望服务在当前用户已授权的任务范围内持续执行，不逐工具、逐动作重复征求批准；这是服务级使用偏好，不是允许任意网页内容扩大任务范围。保留 OAuth/Bearer 身份认证、凭据保护和其他明确禁令。此文字记录不构成第三方运行时的已生效授权，不得伪造 accept、修改官方授权检查或绕过真实拒绝。

核查状态：桥配置已为 trusted_owner_full_access；桥自身 runtime-home 的 approval_policy=never、sandbox_mode=danger-full-access。0.1.10 官方 Computer Use 适配仍需应用访问授权：computer.py 将请求交给客户端，server.py 支持转发或本机人工确认；本次未在公开配置及随附官方 API 文档中找到全应用永久免批准入口。未把上述现有配置或本条用户授权记录冒称为 GUI 已取得全部权限。

用户随后同意为 Browser 使用独立 Playwright/Chromium 适配，并指定 Tabbit 浏览器。该批准只修改 Browser 后端要求；Shell、文件、Git、桌面 GUI 继续使用已验证的官方执行层。Tabbit 使用本项目专属的 AgentDock 浏览器配置目录，不连接日常配置，不复制登录凭据，不依赖 AgentDock 服务。

Remote 稳定性与所有加密请求具体用途已由用户明确移出当前任务范围，按“用户取消验收”记录，历史失败不改写为通过。

2026-09-09 网页账号验收补充：用户已登录并授权继续后，在既有项目“公网验收执行”窗口通过原生 UI 更新连接、刷新工具并提交受控 Browser 实测。该操作没有对日常浏览器附着 CDP、读取或复制 Cookie。生产 Browser 仍严格使用本项目专属 profile。

2026-09-09 后续实现与实测（0.1.11—0.1.12）：用户再次明确要求“默认全部批准不需要授权”，并要求一起修复同类阻塞。服务级配置 auto_approve_application_access=true 使本桥作为 MCP 客户端，按此明确预授权为正常官方应用访问请求返回 accept；来源记为 explicit_owner_preapproval，不伪称用户点击了弹窗。源码、打包 EXE 的 HTTP、stdio、本地直接调用，以及 0.1.12 正式服务本地 HTTP、公网 HTTPS 共八组场景，均取得两次真实 Tabbit 截图和官方许可回执，客户端没有交互授权回调；独立完整回归 226 项通过。上方关于 0.1.10 仍需客户端确认的核查文字保留为历史状态。本次未修改官方运行时或其权限检查，也未把网页/文档内容当作新的用户授权。Clash 与系统网络禁令、身份认证及无模型回合要求持续有效。

