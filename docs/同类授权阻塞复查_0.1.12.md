# 同类授权阻塞复查 · 0.1.12

用户要求：本项目正常应用访问默认全部批准，并检查同类阻塞一起解决。

| 检查项 | 结果与处理 |
| --- | --- |
| 网页 MCP 调用官方 Computer Use | 用户的长期预授权写入 auto_approve_application_access=true。正常官方应用访问表单直接返回 accept，来源 explicit_owner_preapproval，无需网页确认框。 |
| 本地直接 Bridge / CLI 调用 | 查出缺少 MCP forwarder 时直接 cancel 的遗漏；现已复用同一套本地许可解析。 |
| stdio MCP | 与 HTTP 共用许可设置；无交互回调客户端纳入真实截图验收。 |
| HTTP 交互会话 | 默认批准和本地确认模式无需客户端 elicitation，改用无状态 JSON 响应；仅使用独立 Tabbit Browser 时也不无故要求交互会话。 |
| 授权错误归因 | 回调/传输异常明确记录 approval_transport_error，不再只给出 cancel；无交互处理器有单独来源标记，取消决定不能推断为用户点击了取消。 |
| 授权设置可见性 | health/capabilities 在 GUI 启动前即可报告当前 application_access_policy 和 client_elicitation_required。 |
| 普通 Shell、文件、Git、长命令 | 既有官方执行层已经是 dangerFullAccess 与 approval_policy=never，未发现额外人工确认层；通过回归和正式服务 active health 验证。 |
| 按需启动 | 既有 Highest 任务承接授权后的管理员执行，继续保留零自动触发器；不改变系统策略。 |
| 账号连接与刷新 | 原有 OAuth 配置及凭据保留；账号首次配对与应用访问许可是不同机制。未因本次修复要求重新连接账号。 |
| Windows UAC、管理员强制策略 | 未修改或绕开；当前已授权的管理员服务按既有方式执行。 |
| 网页自身业务确认框 | 与官方应用访问许可不同；本次不自动提交业务表单或替视频任务发起生成。独立 Browser 的完整弹窗/上传/下载能力仍属于前一轮第 10 项。 |

真实测试只读取 Tabbit 窗口并截图，不点击业务页面，不启动视频生成，不保存业务截图。源码直接调用、stdio、HTTP，打包程序及正式公网分别保存独立证据；按最终验收记录判断通过，不把仅存在接口的项目算作实测。

配置在本项目 config.toml 根部，服务启动时读取；重启和新客户端继续使用同一设置。收到过期快照或执行结果不确定时仍先重新观察，不重放可能已经执行的动作。HTTP 无状态指协议会话；命令会话、浏览器页和幂等记录仍由服务按原有方式管理。

这次没有开放新的模型 RPC，也没有运行第二个模型作审批。应用访问答复是用户预授权的确定性处理；不接受其他来源冒充用户授权，不自动填写密码/数据表单。Clash、系统代理、DNS、路由及防火墙未修改。

最终验收（2026-09-09）：独立完整回归 226 项通过；源码及打包程序的直接调用、stdio、HTTP，正式服务本地 HTTP、公网 HTTPS，共八组实际场景，每组两次 Tabbit 截图，合计 16 次成功。客户端没有 elicitation 回调，官方回执全部为 accept，来源全部为 explicit_owner_preapproval。HTTP 场景没有协议会话 ID。24 个 Python 模块和 JavaScript worker 与 EXE 一致。

正式服务已安装并运行 0.1.12，最终 health 确认 owner_preapproved、client_elicitation_required=false，Shell 和文件 PASS，管理员子进程令牌验证通过，未观察到未允许的模型 RPC。两个 Highest 按需服务任务均在运行且自动触发器为零。本轮两个临时部署任务经归属检查、保存 XML 后移除，生产任务没有删除。

首轮并行回归曾有一次临时服务退出超过 45 秒的失败（225 项通过、1 项失败），原始报告保留；生命周期单独复查两项在 5.64 秒内通过，随后独立完整复验 226 项在 84.64 秒内通过。首次超时的根因没有得到单独证实；后两次通过不能反写首次结果。对应文件为 owner-preapproval-regression-v0112.xml、owner-preapproval-lifecycle-recheck-v0112.xml、owner-preapproval-regression-final-v0112.xml。

官方依据：[Computer Use 应用许可](https://learn.chatgpt.com/docs/computer-use)、[配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)。

