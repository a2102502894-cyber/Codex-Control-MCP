# Codex-Control-MCP 0.1.8 修复与交付验收

生成时间：2026-09-08T22:35:26+08:00。最终结论以同目录 `evidence/RELEASE_STATUS_0.1.8.json` 为准。

**已经交付可使用的核心服务。原始全部目标尚未达成，不能称为全部完成或绝对无缺陷。** 本报告分别列出已修复问题、实机证据以及仍需外部条件的项目。

## 本次修复

| 问题 | 最终处理 | 证据入口 |
| --- | --- | --- |
| OAuth 冻结异常对象与事务失败 | 改用类事务上下文；失败时正确回滚，不在 SDK 冻结异常上写 traceback | test_oauth_atomic / test_oauth |
| 多线程、多进程重复兑换与配对竞态 | BEGIN IMMEDIATE、嵌套 SAVEPOINT；授权码和配对码一次消费 | multiprocess-oauth.xml / atomic-and-protocol.xml |
| 刷新重放与撤销授权不完整 | 原子签发令牌对；旧刷新令牌重用撤销同族；撤销校验客户端归属 | authorization-boundaries.xml / standard-final.xml |
| 真实浏览器配对提交返回 403 | 授权 HTML 使用 same-origin Referrer-Policy 保留合法 Origin，外域及 null Origin 仍拒绝 | baseline-browser-consent.xml / browser-consent-regression.xml |
| 授权后浏览器拦截 ChatGPT 回调 | form-action 仅增加已严格校验的准确回调地址，保留其他 CSP 限制 | baseline-browser-csp.xml / ChatGPT 实际回调 |
| ChatGPT 只取得短期 control 授权 | 工具明确声明 offline_access 以请求正常刷新授权；已有授权须重新同意 | test_oauth_transport / 账号授权记录 |
| 工具说明包含对调用模型的指令性文字 | 将服务器及命令工具说明改为能力、执行方式、权限和结果等事实；保留危险操作与授权元数据 | 实际 tools/list / 源码与 EXE 一致性 |
| HTTP 请求边界歧义与资源耗尽 | 严格 UTF-8、重复参数/JSON 键、深度/长度/字段数、传输帧及读取期限校验 | test_http_boundaries / test_http_guard |
| 畸形 RPC 响应使待处理请求丢失 | 完整校验后才消费请求；封闭状态拒绝写入，待处理请求有上限 | runtime-regression-r3.xml / standard-final.xml |
| Schema 更新失败损坏可用缓存 | 不可变缓存代次及原子指针；先准备后切换，失败保留旧连接 | cache-regression.xml / 真实双版本切换 |
| 会话嵌套元数据共享、事件增长和启动失败占位 | 深复制、事件上限、缓存读取独立于重连、失败不消耗会话配额 | standard-final.xml / core-standard.xml |
| 异常或断线导致副作用重发 | 保持明确未知状态、持久幂等摘要和禁止自动重放 | 故障回归及实际集成报告 |
| 后台进程窗口、任务归属与输出目录启动 | GUI subsystem 宿主；启动前校验两项任务、SHA、版本、SID、零触发器 | background-host.json / service-lifecycle-v018.json |
| 正常退出被停止命令提前报失败 | 等待窗口覆盖 HTTP 排空和所属官方进程关闭两阶段；保留真实退出状态 | 两轮 EXE 宿主、源码/OAuth/实际启停 |
| 构建相对路径写入错误目录 | 在调用方工作目录先解析输出绝对路径，再构建和检查同一文件 | windows-build.json / source-runtime-verification.json |
| 测试证据并行覆盖 | 标准和管理员集成报告、子进程记录使用独立 profile 文件 | core-standard.xml / core-admin.xml |
| 负向注册测试污染正在进行的真实配对 | 注册负向探针先确认配对窗口关闭；删除了唯一经核对的本次误建测试客户端 | https-acceptance-v018.json / 本机账户审计 |
| 许可与源码包缺失/重复 | 保留完整 34 份第三方许可，去除完全重复的嵌套目录，打包完整源码与校验清单 | SOURCE_MANIFEST.json / THIRD_PARTY_NOTICES.md |
| 缺乏所属进程网络证据 | 增加可复验 ETW 和本地连接阳性对照；额外连接及无法判断的用途如实报告 | owned-network-*.json |

历史失败 XML 保留真实用例名称及失败数量，交付副本删除可能含有合成凭据的失败正文。更详细原始测试日志仅保留本机；没有重写历史失败为通过。

## 实际验收

| 范围 | 结果与范围 |
| --- | --- |
| 普通 Windows 权限 | 179 passed，1 个 WSL 用例排除后另报；`standard-final.xml` |
| 管理员 Windows 权限 | 179 passed，1 个 WSL 用例另报；实际验证官方子进程继承管理员令牌 |
| 打包 EXE | 独立 OAuth HTTP → 官方执行 → 回传通过；单独的 EXE 核心流程、拒绝模型 RPC、UTF-8 文件和 Git 验证通过 |
| EXE 与源码一致性 | 23 个模块的 code object 逐一相同；不是只对版本字符串 |
| 后台和启停 | 两轮真实宿主周期、所属进程无可见控制台窗口、正常关闭；已安装和输出目录启动脚本分别验证 |
| 公网入口 | 独立 TLS 隧道、OAuth discovery、未授权拒绝、关闭注册窗口、23 个工具、实际命令及 UTF-8 文件往返 |
| 系统代理 | 官方 command/exec 子进程经本机 HTTP CONNECT 代理访问微软 HTTPS，证书验证和实际 socket 对端均核对 |
| 版本兼容 | 实际官方 CLI 0.153.3 → 0.153.4，官方归档校验、发现切换和文件往返；Desktop 安装不变 |
| ChatGPT | 真实 ChatGPT OAuth 连接、23 个工具发现和公网执行回传均已通过（账号实测版本 0.1.8），重新授权后已核实真实 offline_access 刷新授权签发 |
| 所属进程网络 | ETW 两轮有效，inherited：21 个所属进程、31 条网络事件；classify：21 个所属进程、54 条网络事件；事件丢失 0，连接阳性对照成功 |

网络诊断没有解密 TLS，没有记录包正文、授权值或其他进程的详情。原始代理运行和“只记录 CONNECT 目标并拒绝转发”的诊断运行分开，后者不能冒充原始联网行为。模型测试端点的零请求是已经观察的结果，额外加密连接的具体用途保留 UNVERIFIED。

## 尚未完成的项目

| 验收项 | 状态 | 原因和继续条件 |
| --- | --- | --- |
| chatgpt_real_account_execution | PASS | 以账号侧工具实际返回及本地执行审计核验。OAuth 连接和工具发现已完成，执行结果单独记录。 |
| official_browser_actions | UNAVAILABLE | 独立官方 Browser 调用返回 Missing required Codex turn metadata: session_id, turn_id。没有合法的无模型宿主回合通道，未伪造或借用 Desktop 的标识。 |
| official_computer_actions | USER_APPLICATION_CONSENT_REQUIRED | 官方窗口枚举与全动作验收分别记录；应用访问需要实际用户授权。关闭或超时保持取消。 |
| wsl_actual_execution | WINDOWS_COMPONENT_RESTART_REQUIRED | 已安装微软 WSL 2.7.13、启用组件并校验官方 Debian 镜像；本次系统尚未重启，实际导入返回 WSL_E_WSL1_NOT_SUPPORTED。准备使用独有的 WSL1 测试发行版。 |
| full_chain_no_model_network | UNVERIFIED | 两次所属进程 ETW 观测通过且零丢事件，模型测试端点零请求；官方进程另有 ChatGPT/GitHub 的 HTTPS 连接。连接域名不能证明加密请求的具体用途。 |
| remote_websocket_and_gui_proxy | NOT_TESTED | 实际命令子进程通过系统 HTTP CONNECT 代理访问微软 HTTPS 已通过；这不替代官方 Remote/WebSocket 和 Browser/Computer Use 的分别验收。 |

WSL：微软签名安装包已验证并安装，组件启用明确返回需重启；Debian 镜像 SHA-256 为 `5ec7dc68216e75d1d4d4761474e99d8461a98d316537110314b137122a879e0f`。当前固件未开启虚拟化，因此续验采用 WSL1，不要求为本测试修改 BIOS。**本次没有重启 Windows、没有自动续跑任务，也没有把安装成功等同于 Linux 命令已执行。**

GUI：窗口枚举不等于截图、点击、输入、滚动均可用。只有真实用户应用授权与全部受控动作完成后才能标 PASS。官方 Browser 需要的有效宿主回合标识无法由桥接伪造，也不能为了通过测试启动模型回合。

## 安装、回退与完整性

当前版本使用独立目录，旧版本保留。原工程回写前按 106 文件基线逐一检测外部修改，并生成 `Source_before_0.1.8.zip`、`SOURCE_DIFF.patch`；回写结果看 `evidence/source-sync.json`。现有运行配置、DPAPI 授权数据库、Desktop 配置和其他软件的数据不进入公开交付包。

可执行文件、源码、完整许可和安全筛选后的验收记录在 Windows delivery ZIP 中。`SHA256SUMS.txt` 校验目录文件，`SOURCE_MANIFEST.json` 校验源码。未签名事实见 `windows-build.json`，官方 Codex 不包含在分发包中。

## 核对过的官方资料

- [OAuth 授权响应中的浏览器 Origin 规则](https://fetch.spec.whatwg.org/#origin-header)
- [Referrer-Policy](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Referrer-Policy)
- [CSP form-action 与表单跳转](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Security-Policy/form-action)
- [微软 WSL 手动安装与重启要求](https://learn.microsoft.com/en-us/windows/wsl/install-manual)
- [微软 TraceEvent 3.2.6](https://www.nuget.org/packages/Microsoft.Diagnostics.Tracing.TraceEvent/3.2.6)

运行时能力以本机官方 CLI、真实 Schema 和实际返回为准，网上主干接口不能代替当前安装版本验收。

